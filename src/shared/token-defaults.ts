/** Default context window if model info is unknown. */
export const DEFAULT_CONTEXT_WINDOW = 131072;

/** Default token limit if model info is unknown. */
export const DEFAULT_MAX_OUTPUT_TOKENS = 65536;
