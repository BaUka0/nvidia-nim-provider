// Re-export all constants from the new location for backward compatibility
export {
  PROVIDER_VENDOR,
  PROVIDER_DISPLAY_NAME,
  SECRET_STORAGE_KEY,
  RAW_MODELS_STATE_KEY,
  MODELS_STATE_KEY,
  MODELS_CACHE_VERSION_STATE_KEY,
  MODELS_CACHE_VERSION,
  MIGRATION_DONE_KEY,
  DEBUG_STATE_KEY,
  DEBUG_ENV_VAR,
  MANAGE_COMMAND_ID,
  REFRESH_MODELS_COMMAND_ID,
  TOGGLE_DEBUG_LOGGING_COMMAND_ID,
  OPEN_DEBUG_LOG_COMMAND_ID,
  TOGGLE_SHOW_REASONING_COMMAND_ID,
  BASE_URL,
  EXTENSION_VERSION,
  CONTEXT_WINDOW_SAFETY_MARGIN,
  DEFAULT_MAX_OUTPUT_TOKENS,
  MAX_RETRY_DELAY_MS,
  BASE_RETRY_DELAY_MS,
  STREAM_IDLE_TIMEOUT_MS,
  STREAM_IDLE_TIMEOUT_MIN_MS,
  STREAM_IDLE_TIMEOUT_MAX_MS,
  STATUS_BAR_DEFAULT_TEXT,
  STATUS_BAR_ERROR_TEXT,
} from "./shared/constants";
